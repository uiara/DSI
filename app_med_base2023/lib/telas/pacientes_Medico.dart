import 'package:flutter/material.dart';

class PacMedico extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pacientes do Médico'),
      ),
      body: Center(
        child: Text('em construção'),
      ),
    );
  }
}
