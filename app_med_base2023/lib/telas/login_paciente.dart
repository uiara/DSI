import 'package:flutter/material.dart';

class PacientePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Página do Paciente'),
      ),
      body: Center(
        child: Text('Esta é a página do paciente'),
      ),
    );
  }
}
